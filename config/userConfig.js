// our SQL connect info
const sqlCreds = {
    user: "root",
    password: "root",
    host: "localhost",
    port: 8889, // note: this is pertaining to your SQL port / check your creds in PHP MyAdmin.
    database: "db_roku"
}

module.exports = sqlCreds;